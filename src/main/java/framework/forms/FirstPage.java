package framework.forms;

import framework.base.BaseForm;
import org.openqa.selenium.By;

public class FirstPage extends BaseForm {
    public FirstPage(By locator, String name) {
        super(locator, name);
    }
}