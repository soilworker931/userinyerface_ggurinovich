package framework.forms;

import framework.base.BaseForm;
import org.openqa.selenium.By;

public class SecondCard extends BaseForm {

    public SecondCard(By locator, String name) {
        super(locator, name);
    }
}